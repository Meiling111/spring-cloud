server:
  port: 8763

eureka:
  client:
    register-with-eureka: false #防止eureka-server自己注册自己
    fetch-registry: false #只维持服务对象，不查找服务
    serviceUrl:
       defaultZone: http://localhost:${server.port}/eureka/

   
   
   
   